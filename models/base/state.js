// Configure logger for Tools
var logger = new Logger('Models:State');
// Comment out to use global logging level
Logger.setLevel('Models:State', 'trace');
// Logger.setLevel('Models:State', 'debug');
// Logger.setLevel('Models:State', 'info');
// Logger.setLevel('Models:State', 'warn');

UserState = function(stateVars) {
  /******************************************************************
   * State object to persist a set of user state variables to support
   * state machine like functionality 
   * 
   *
   * @params
   *    stateVars - an object where the variable is the key
   *        and the value is the relevant state information to ref.
   *****************************************************************/
  
}
